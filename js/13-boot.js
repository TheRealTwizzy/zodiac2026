/* ============================================================================
   BOOT — runs last, after every other part has defined its globals and the
   DOM is in place. Nothing else calls these.
   ==========================================================================*/

bootSite();
wireTour();
